# sensitive configuration (real Sheet ID + personal emails)
config.py

# local deployment path (would leak the local username into git)
install.ini

# python
__pycache__/
*.pyc
venv/

# local cache of API-fetched license details (regenerable, not archival)
cache/

# generated data (regenerated by the bot; contains license/personal data)
temp_full.*
diff_*.csv
weekly_diff_*.csv
last_mail_*.html
last_weekly_report.html
temp_mail_*.html
temp_weekly_*.html

# legacy subscriber exports, if regenerated
yeela.csv
sheet_export.csv

# local Claude Code permission settings
.claude/settings.local.json
